export type Device = {
    id: string;
    project: string;
    startDate: string;
    endDate: string;
    lastUpload: string;
    batteryLevel: number;
    folderSize: string;
    action: string;
};